spring:
  application:
    name: api-gateway
  sleuth:
    sampler:
      probability: 1.0 # TRACE 100% REQUESTS
  #cloud:
   # gateway:
    #  discovery:
     #   locator:
      #    enabled: true
       #   lowerCaseServiceId: true


server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka